""" Funciones en Python

En Python una funcion es un codigo reutilizable que toma argumentos como input, realiza algunos calculos y luego devuelve 
uno o mas resultados.

En python existen dos tipos de funciones:

** Incorporadas: Que se presentan como parte de Python. se pueden consultar en https://docs.python.org/3/library/functions.html:

                    ejemplo de funciones:
                    print()
                    int()
                    type()
                    

** Definidas:  Son aquellas que son creadas por el usuario y se invocan cuando sea necesario.
                Se utiliza la palabra reservada 'def' para definir la nueva funcion:

                -- def + nombre + (parametros):
                        return                   --
"""
# Ejemplo

def suma (x, y):
    return x + y

"""

Definiciones

*** Parametros: es una o varias variables que utilizamos en la funcion.  


*** Argumentos: es un valor que informamos a la funcion como su entrada cuando llamamos a la funcion

"""

